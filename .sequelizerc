const path = require('path');
const fs = require("fs");
const config = require('./dist/app/configs/config');

fs.writeFileSync('./dist/sequelize.json', JSON.stringify(config.config.sequelize), 'utf8');

module.exports = {
  "config": path.resolve('./dist/sequelize.json'),
  "models-path": path.resolve('./dist/app/models'),
  "seeders-path": path.resolve('./dist/seeders'),
  "migrations-path": path.resolve('./dist/migrations')
};
