const path = require('path');
const fs = require("fs");
const config = require('./dist/configs/config');

fs.writeFileSync('./dist/configs/sequelize.json', JSON.stringify(config.config.sequelize), 'utf8');

module.exports = {
  "config": path.resolve('./dist/configs/sequelize.json'),
  "models-path": path.resolve('./dist/models'),
  "seeders-path": path.resolve('./dist/seeders'),
  "migrations-path": path.resolve('./dist/migrations')
};
