language: node_js
node_js:
  - "node"
branches:
  only:
    - master
    - development
install:
  - npm install
script:
  - npm run-script lint
  - npm run-script test
after_success:
  - "curl -u deploy:$DEPLOY_PASSWORD https://deploy.awix.io/deploy/api/$TRAVIS_BRANCH"
